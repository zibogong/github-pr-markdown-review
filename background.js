// Background worker — kept minimal, only used for chrome.storage access
// if needed by other extension features in the future.
// All GitHub interaction now happens via same-origin fetch in content.js.
